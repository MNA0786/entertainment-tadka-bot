# Docker related
docker/
docker-compose.override.yml

# Environment files
.env
.env.local

# Log files
*.log
/logs/

# Backup files (actual data, not directory)
/backups/*.csv
/backups/*.json
/backups/*.bak
!/backups/.gitkeep

# Data files (templates ko chhod ke)
/movies.csv
/users.json
/bot_stats.json
/channels_tracker.json
/error.log

# But keep empty templates (agar banaye to)
!/movies.csv.empty
!/users.json.empty
!/bot_stats.json.empty
